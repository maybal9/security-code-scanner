using System;

namespace security_code_scanner
{
    class Program
    {
        static void Main(string[] args)
        {
            // string input_path;
            Scanner scanner = new Scanner();
            // scanner.Scan(input_path, "text");
            // scanner.Scan(input_path, "json");
        }
    }
}
